(ns hangman.test
 (:require clojure.test)
 (:gen-class
    :methods [#^{:static true} [foo [int] void]]))

;; (deftest addition
;;   (is (= 4 (+ 2 2)))
;;   (is (= 7 (+ 3 4))))

;; (deftest subtraction
;;   (is (= 1 (- 4 3)))
;;   (is (= 3 (- 666 4))))

